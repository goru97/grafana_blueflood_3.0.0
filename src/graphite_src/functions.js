class functions{
    constructor(){
       super();
    }
    not_empty(values) {
        values.forEach(v =>{
        if (v !== null){
            return True
        }
        return False
        });
    }


   /* safe(f) {
        var inner = function (values){
            vals = list(not_none(values))
            if not vals
            return
        return f(vals)
        return inner
    }*/
}
